<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="admin">

	<!-- 관리자 번호 닉네임 변환  -->
	<select id="conversionNickname" parameterType="int" resultType="String">
		SELECT ADMIN_NICKNAME
		FROM UB_ADMIN 
		WHERE ADMIN_NUMBER = #{adminNumber}
	</select>
	
	
	<!-- 관리자 로그인 -->
	<select id="adlogin" parameterType="AdminDTO" resultType="int">
		SELECT * FROM UB_ADMIN
		WHERE admin_id = #{adminId} and admin_pw = #{adminPw}
	</select>
	
	<!-- 메인페이지  -->
	<select id="membertotal" resultType="int">
		SELECT count(*) FROM UB_MEMBER
	</select>
	
	<select id="mentortotal" resultType="int">
		SELECT count(*) FROM UB_MEMBER WHERE MEMBER_TYPE = 'MENTOR'
	</select>
	
	<select id="menteetotal" resultType="int">
		SELECT count(*) FROM UB_MEMBER WHERE MEMBER_TYPE = 'MENTEE'
	</select>
	
	<select id="matchingtotal" resultType="int">
		SELECT count(*) FROM UB_MATCHING WHERE MATCHING_STATE = '매칭중'
	</select>
	
	
	<select id="recentboard" resultType="AdMainDTO">
	<![CDATA[
		SELECT * 
		FROM (
		SELECT  umb.board_title, umb.board_date
		FROM UB_MENTEE_BOARD umb
		UNION ALL 
		SELECT  umb.board_title, umb.board_date
		FROM UB_MENTOR_BOARD umb
		ORDER BY board_date)
		WHERE ROWNUM <= 5
	]]>
	</select>
	
	<select id="recentMember" resultType="AdMainDTO">
	<![CDATA[
		SELECT * 
		FROM (
		SELECT MEMBER_NAME, MEMBER_type, member_date 
		FROM UB_MEMBER
		ORDER BY member_date)
		WHERE ROWNUM <= 5
	]]>
	</select>
	
	<!-- 멘티 게시판 -->
	<!-- 전체 조회 게시판 갯수 -->
	<select id="menteeGetTotal" resultType = "int">
		select count(*) from UB_MENTEE_BOARD
	</select>
	
	<!-- 랜더링 조회 게시판 갯수 -->
	<select id="menteeGetRenderingTotal" resultType = "int">
		select count(*) from UB_MENTEE_BOARD 
		WHERE board_date BETWEEN TO_DATE(#{dateFrom}, 'YYYY-MM-DD') AND TO_DATE(#{dateTo}, 'YYYY-MM-DD') 
	</select>
	
	<!-- 전체 조회 -->
	<select id="menteeSelectAll" resultType = "AdMenteeBoardListDTO">
		<![CDATA[
			SELECT * FROM (
				SELECT ROWNUM AS rnum, subquery.*
				FROM (
					SELECT umb.menteeboard_number, board_title, board_click, board_date, 
						CASE
							WHEN umb.admin_number IS NOT NULL THEN ua.admin_Nickname
							WHEN umb.MEMBER_number IS NOT NULL THEN um.MEMBER_nickname
						END AS "write_nickname"
					FROM UB_MENTEE_BOARD umb
					LEFT JOIN UB_MEMBER um ON um.MEMBER_NUMBER = umb.MEMBER_NUMBER 
					LEFT JOIN UB_ADMIN UA ON umb.admin_number = ua.admin_number
					ORDER BY umb.MENTEEBOARD_NUMBER desc
				) subquery
			)WHERE rnum BETWEEN #{startRow} AND #{endRow}
		]]>
	</select>
	
	<!-- 멘티 랜더링 조회 -->
	<select id="menteeSelectFilter" resultType="AdMenteeBoardListDTO">
		<![CDATA[
			SELECT * FROM (
				SELECT ROWNUM AS rnum, subquery.*
				FROM (
					SELECT umb.menteeboard_number, board_title, board_click, board_date, 
						CASE
							WHEN umb.admin_number IS NOT NULL THEN ua.admin_Nickname
							WHEN umb.MEMBER_number IS NOT NULL THEN um.MEMBER_nickname
						END AS "write_nickname"
					FROM UB_MENTEE_BOARD umb
					LEFT JOIN UB_MEMBER um ON um.MEMBER_NUMBER = umb.MEMBER_NUMBER 
					LEFT JOIN UB_ADMIN UA ON umb.admin_number = ua.admin_number
					WHERE board_date BETWEEN TO_DATE(#{dateFrom}, 'YYYY-MM-DD')
					AND TO_DATE(#{dateTo}, 'YYYY-MM-DD')
					ORDER BY umb.MENTEEBOARD_NUMBER desc
				) subquery
			)WHERE rnum BETWEEN #{startRow} AND #{endRow}
		]]>
	</select>
	
	<!-- 멘티 상세 조회 -->
	<select id="menteeSelectOne" resultType="AdMenteeBoardDTO">
		SELECT UMB.menteeboard_number, UMB.BOARD_TITLE , UMB.BOARD_CONTENT , UMB.BOARD_CONTENT , UMB.BOARD_CLICK , UMB.board_date, UF.FILE_NAME, 
		CASE
			WHEN umb.admin_number IS NOT NULL THEN ua.admin_Nickname
			WHEN umb.MEMBER_number IS NOT NULL THEN um.MEMBER_nickname
		END AS "write_nickname",
		CASE
			WHEN umb.admin_number IS NOT NULL THEN ua.admin_number
			WHEN umb.MEMBER_number IS NOT NULL THEN um.MEMBER_number
		END AS "write_number"
		FROM  UB_MENTEE_BOARD UMB
		LEFT JOIN UB_MEMBER UM  ON UMB.member_number = UM.member_number
		LEFT JOIN UB_ADMIN UA ON UMB.admin_number = UA.admin_number
		LEFT JOIN UB_FILE UF ON UMB.FILE_NUMBER = UF.FILE_NUMBER
		WHERE UMB.MENTEEBOARD_NUMBER = #{boardNumber}
	</select>
	
	<!-- 멘티 게시판 작성 -->
	<insert id="insert" parameterType="AdMenteeBoardDTO">
		<selectKey keyProperty="menteeboardNumber" resultType="int" order="BEFORE">
			SELECT SEQ_MENTEE_BOARD.nextVal FROM DUAL
		</selectKey>
		INSERT INTO UB_mentee_board(menteeboard_number, board_title, board_content, admin_number)
		VALUES(#{menteeboardNumber}, #{boardTitle}, #{boardContent}, #{writeNumber})
	</insert>
	
	<!-- 멘티 게시판 수정 -->
	<update id="menteeUpdate">
		UPDATE UB_mentee_board 
		SET board_title=#{boardTitle}, board_content=#{boardContent} 
		WHERE menteeBoard_number=#{menteeboardNumber}
	</update>
	
	<!-- 멘티 게시판 삭제 -->
	<delete id="menteeDelete">
		delete From UB_mentee_board
		where menteeBoard_number=#{menteeboardNumber}
	</delete>
	
	<!-- 멘티 게시판 댓글 전체 삭제 -->
	<delete id="menteeDeleteAllComment">
		DELETE FROM UB_mentee_board_comment WHERE menteeBoard_ID = #{menteeboardNumber}
	</delete>

	
	<!-- 멘토 게시판 -->
	<select id="mentorGetTotal" resultType = "int">
		select count(*) from UB_MENTOR_BOARD
	</select>
	
	<select id="mentorSelectAll" resultType = "AdMentorBoardListDTO">
		<![CDATA[
			SELECT * FROM (
				SELECT ROWNUM AS rnum, subquery.*
				FROM (
					SELECT umb.mentorboard_number, board_title, board_click, board_date, 
						CASE
							WHEN umb.admin_number IS NOT NULL THEN ua.admin_Nickname
							WHEN umb.MEMBER_number IS NOT NULL THEN um.MEMBER_nickname
						END AS "write_nickname"
					FROM UB_MENTOR_BOARD umb
					LEFT JOIN UB_MEMBER um ON um.MEMBER_NUMBER = umb.MEMBER_NUMBER 
					LEFT JOIN UB_ADMIN UA ON umb.admin_number = ua.admin_number
					ORDER BY umb.MENTORBOARD_NUMBER desc
				) subquery
			)WHERE rnum BETWEEN #{startRow} AND #{endRow}
		]]>
	</select>
	
	<select id="mentorSelectFilter" resultType="AdMentorBoardListDTO">
		<![CDATA[
			SELECT * FROM (
				SELECT ROWNUM AS rnum, subquery.*
				FROM (
					SELECT umb.mentorboard_number, board_title, board_click, board_date, 
						CASE
							WHEN umb.admin_number IS NOT NULL THEN ua.admin_Nickname
							WHEN umb.MEMBER_number IS NOT NULL THEN um.MEMBER_nickname
						END AS "write_nickname"
					FROM UB_MENTOR_BOARD umb
					LEFT JOIN UB_MEMBER um ON um.MEMBER_NUMBER = umb.MEMBER_NUMBER 
					LEFT JOIN UB_ADMIN UA ON umb.admin_number = ua.admin_number
					WHERE board_date BETWEEN TO_DATE(#{dateFrom}, 'YYYY-MM-DD')
					AND TO_DATE(#{dateTo}, 'YYYY-MM-DD')
					ORDER BY umb.MENTORBOARD_NUMBER desc
				) subquery
			)WHERE rnum BETWEEN #{startRow} AND #{endRow}
		]]>
	</select>
	

	
</mapper>
